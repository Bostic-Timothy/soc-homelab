# Provisioning

Planned location for reproducible Proxmox VM, template, network, and snapshot provisioning.

Add implementation only after it has been tested against the range. Keep provider credentials, state files, host addresses, and private inventory outside version control.
